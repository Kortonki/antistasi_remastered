class AS_fuel {

    class server {

      FNC(fuel,changeFIAfuelReserves);

    };

    class common {

      FNC(fuel,getFuelCargo);
      FNC(fuel,getFuelTankSize);
      FNC(fuel,getVehicleFuel);
      FNC(fuel,setVehicleFuel);
      FNC(fuel,addVehicleFuel);
      FNC(fuel,setFuelCargo);
      FNC(fuel,addFuelCargo);
      FNC(fuel,randomFuelCargo);
      FNC(fuel,getFuelCargoSize);
      FNC(fuel,returnTripFuel)
    };

  };
